interface System {
	deployModules: () => void;

	intentSearch: (str: string) => void;
	search: (str: string) => void;
}

const System: System = {
	deployModules: () => {

	},

	intentSearch: (str: string) => {

	},
	search: (str: string) => {

	}
};
